// A SİYAHISI — LEAF seqment, naməlum filtr 404 vermir. İzah:
// `(admin)/loading.tsx`.
import { PageSkeleton } from "@/components/shared/PageSkeleton";

export default function FaqLoading() {
  return <PageSkeleton variant="rows" label="Suallar yüklənir" />;
}
